
name: Building

on:
  push:
    branches: [ "aula" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: docker login
      run: docker login -u pedro -p xpto
    - name: build image 
      run: docker build -t user/ping .